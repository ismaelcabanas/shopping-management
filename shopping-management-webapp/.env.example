# ============================================================================
# OCR Provider Configuration
# ============================================================================
# Select which OCR provider to use for ticket scanning
# Options: gemini | ollama | mock
# Default: gemini (if not set)
#
# - gemini: Cloud-based, requires API key, 60 requests/min free tier
# - ollama: Local/offline, requires Ollama running, free and unlimited
# - mock: Testing only, returns hardcoded data
VITE_OCR_PROVIDER=gemini

# ----------------------------------------------------------------------------
# Gemini Configuration (when VITE_OCR_PROVIDER=gemini)
# ----------------------------------------------------------------------------
# Cloud OCR provider - FREE tier: 60 requests/minute
# Get your API key at: https://makersuite.google.com/app/apikey
#
# MIGRATION NOTE: Variable names changed for provider abstraction
# - Old: VITE_GEMINI_API_KEY → New: VITE_OCR_LLM_API_KEY
# - Old: VITE_GEMINI_MODEL → New: VITE_OCR_LLM_MODEL
# (Despite generic names, currently only Gemini is supported as LLM provider)
VITE_OCR_LLM_API_KEY=your-gemini-api-key-here
VITE_OCR_LLM_MODEL=gemini-2.0-flash

# ----------------------------------------------------------------------------
# Ollama Configuration (when VITE_OCR_PROVIDER=ollama)
# ----------------------------------------------------------------------------
# Local OCR provider - FREE, unlimited, offline
# Requires Ollama installed and running: https://ollama.com
# Start server: ollama serve
# Pull model: ollama pull llava
VITE_OLLAMA_URL=http://localhost:11434
VITE_OLLAMA_MODEL=llava

# App Configuration (opcional)
# VITE_APP_TITLE=Shopping Manager
# VITE_APP_VERSION=1.0.0